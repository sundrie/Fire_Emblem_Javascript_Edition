$(function(){

    var config = {
        type: Phaser.AUTO,
        width: 800,
        height: 480,
        scene: {
            preload: preload,
            create: create,
            update: update
        }
    };
    var game = new Phaser.Game(config);

    // Charge les images, sons, textures, etc
    function preload(){

    };

    function create(){

    };

    function update (){

    };
});